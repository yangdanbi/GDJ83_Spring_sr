<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<beans:bean class="com.sse.app.interceptors.WriterInterceptor" name="writerInterceptor"></beans:bean>
	<interceptors>
		<interceptor>
		<!--어떤 url이 왔을때  -->
		<!-- mapping -> 어느객체를 할 것인지/ 이 순서대로 작성해야함 -->
			<mapping path="/product/*"/>
			<!--제외하고 싶은것 여러개 제외 가능 -->
			<exclude-mapping path="/product/add"/>
			<beans:ref bean="testInterceptor"/>
		</interceptor>
		<interceptor>
			<mapping path="/product/*"/>
			<mapping path="notice/*"/>
			<exclude-mapping path="/product/list"/>
			<exclude-mapping path="/product/detail"/>
			<exclude-mapping path="/notice/list"/>
			<exclude-mapping path="/notice/detail"/>
			<!--어노테이션 없이 여기서 객체를 만들어서 쓰겠다 -->
			<beans:bean class="com.sse.app.interceptors.AdminInterceptor"></beans:bean>
		</interceptor>
		
		<interceptor>
			<mapping path="/qna/update"/>
			<beans:ref bean="writerInterceptor"/>
		
		</interceptor>
	
	</interceptors>
	
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	 	<beans:property name="order" value="1"></beans:property>
	</beans:bean>
	
	<beans:bean class="org.springframework.web.servlet.view.BeanNameViewResolver">
		<beans:property name="order" value="0"></beans:property>
	</beans:bean>
	
	<context:component-scan base-package="com.sse.app" />
	
	
	
</beans:beans>
